@model CreateVoucherViewModel

@{
    ViewData["Title"] = "Create";
}

<div class="container" style="min-height:800px;">
    <h1 class="text-center">Create Voucher</h1>

    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create">
                <div class="form-group">
                    <label asp-for="@Model.Code" class="control-label"></label>
                    <input asp-for="@Model.Code" class="form-control" />
                    <span asp-validation-for="@Model.Code" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Vpercent" class="control-label">Amount of discount</label>
                    <input asp-for="@Model.Vpercent" class="form-control" />
                    <span asp-validation-for="@Model.Vpercent" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Quantity" class="control-label"></label>
                    <input asp-for="@Model.Quantity" class="form-control" />
                    <span asp-validation-for="@Model.Quantity" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.StartDate" class="control-label"></label>
                    <input asp-for="@Model.StartDate" class="form-control" />
                    <span asp-validation-for="@Model.StartDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.EndDate" class="control-label"></label>
                    <input asp-for="@Model.EndDate" class="form-control" />
                    <span asp-validation-for="@Model.EndDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="UserId" class="control-label">Dành cho người dùng:</label>
                    <br />
                    <select class="select2" id="userList"
                            multiple="multiple"
                            data-placeholder="Chọn người dùng..."
                            asp-for="UserIds"
                            asp-items="Model.Users">
                    </select>
                    <br />
                    <p class="font-italic mt-3 text-muted" style="font-size: 13px;">
                        Lưu ý: Voucher sẽ có hiệu lực cho toàn bộ người dùng trong hệ thống nếu không có người dùng nào được chọn.
                    </p>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                    <a class="btn btn-success ml-2" asp-action="Index">Back to List</a>
                </div>
            </form>
        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        $('select.select2').each(function () {
            if ($(this).hasClass('select2-hidden-accessible')) {
                $(this).select2('destroy');
            }

            $(this).select2({
                allowClear: true,

                width: '50%'
            });
        });
    </script>
}
